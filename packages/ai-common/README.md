# @cross-repo-libs/ai-common

Small shared helpers for reusable AI generation packages:

- path resolution
- JSON job loading
- existing-path validation
- dry-run subprocess execution

This package intentionally has no app-specific asset, audio, or image assumptions.
